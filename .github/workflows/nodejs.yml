name: Node CI

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x]
        node-path: [./node-project]

    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Cache node modules
      uses: actions/cache@v1
      with:
        path: ${{ matrix.node-path }}/node_modules
        key: npm-${{hashFiles('${{ matrix.node-path }}/package-lock.json')}}
    - name: npm install, build, and test
      run: |
        npm ci
        npm install
        npm run build --if-present
        npm run test
      working-directory: ${{ matrix.node-path }}
      env:
        CI: true
